spring:
  sql:
    init:
      mode: never
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-id
            client-secret: test-secret
            scope:
              - email
              - profile
jwt:
  issuer: jmpark6846@naver.com
  secret_key: test-secret
